#include<iostream>
using namespace std;
class operations
{
	float a,b,c,d;
	public:
	float add(x,y);
	float sub(x,y);
	float multiply(x,y);
	float divide(x,y);
};
int main()
{
	int op;
	cout<<"Enter operation"(+ - * and %);
	cin>>op;
	if(op == +)
	{add(6,7);
		break;
	}
	if(op == -)
	{ sub(8,5);
		break;
	}
	if(op == *)
	{
		multiply(7,4);
		break;
	}
	if(op == %)
	{divide(6,3);
		break;
	}
}
float add(x,y)
{
	a=x+y;
	return a;
}
float sub(x,y)
{
	b=x-y;
	return b;
}
float multiply(x,y)
{ c=x*y;
	return c;
}



