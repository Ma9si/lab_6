#include<iostream>
using namespace std;
class operations
{
	float a,b;
	public:
	float add(x,y);
	float sub(x,y);
	float multiply(x,y);
	float divide(x,y);
};
int main()
{
	int op;
	cout<<"Enter operation"(+ - * and %);
	cin>>op;
	if(op == +)
	{add(6,7);
		break;
	}
	if(op == -)
	{ sub(8,5);
		break;
	}
	if(op == *)
	{
		multiply(7,4);
		break;
	}
	if(op == %)
	{divide(6,3);
		break;
	}
}


